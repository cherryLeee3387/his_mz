server:        #端口号
  port: 8080
spring:
  application:
    name: menzhen
eureka:      #eureka地址
  client:
    service-url:
      default-zone: http://127.0.0.1:8761/eureka
feign:        #启动feign的熔断器功能
  hystrix:
    enabled: true